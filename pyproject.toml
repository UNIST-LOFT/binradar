[project]
name = "fuzzolic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.3.5",
    "sbsv>=0.2.3,<0.3.0",
    "sortedcontainers>=2.4.0",
]
